{
  "id": "schedule-inspection-event",
  "description": "Structured address assembly + user email from conversation + city.",
  "tool": {
    "name": "scheduleInspectionEvent",
    "description": "Schedule a maintenance inspection event.",
    "parameters": {
      "siteAddress":    { "type": "string", "description": "Site street address, single line in 'Street Number' format." },
      "siteCity":       { "type": "string" },
      "organizerEmail": { "type": "string", "format": "email", "description": "User's email address (from the conversation)." }
    }
  },
  "expected": {
    "mappings": {
      "siteAddress":    "Maximilianstrasse 42",
      "siteCity":       "Muenchen",
      "organizerEmail": "marc@the-real-insight.com"
    }
  }
}
